dispatcher:
  email: dispatcher_fixture@test.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password") %>
  role: dispatcher

driver_user:
  email: driver_fixture@test.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password") %>
  role: driver
  driver: two
